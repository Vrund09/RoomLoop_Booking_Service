[pytest]
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    # Newer Starlette nags that its TestClient uses httpx; third-party, not ours.
    ignore:Using `httpx` with `starlette.testclient` is deprecated:UserWarning
